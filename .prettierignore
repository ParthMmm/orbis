bun.lock
node_modules/
.turbo/
dist/
out/
.vite/

# The glossary keeps each term and its avoided synonyms on separate lines on purpose.
CONTEXT.md

# Generated by scripts/design-tokens.mjs; the token check compares it byte for byte.
docs/design/tokens.css

# Xcode's asset catalog editor owns these files and writes `"key" : value`, which the
# formatter would strip on every pass and Xcode would put back on every edit.
**/*.xcassets/**

# Agent tooling state, not product source.
.agents/
.claude/
.pi/
skills-lock.json

# Domain docs

Use one root `CONTEXT.md` glossary and `docs/adr/` for decisions.

Before exploring domain behavior, read the glossary and relevant ADRs when present. Create them lazily through the domain-modeling skill; their absence is not a blocker.

Use glossary terminology consistently in specs, issues, tests, and code. Flag conflicts with ADRs rather than silently overriding them.
